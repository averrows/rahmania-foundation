---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class={`py-20 px-4 sm:px-6 md:px-8 ${className}`}>
  <div class="container mx-auto">
    <slot />
  </div>
</section>
